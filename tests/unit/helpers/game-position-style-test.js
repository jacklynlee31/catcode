import { gamePositionStyle } from '../../../helpers/game-position-style';
import { module, test } from 'qunit';

module('Unit | Helper | game position style');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = gamePositionStyle(42);
  assert.ok(result);
});
