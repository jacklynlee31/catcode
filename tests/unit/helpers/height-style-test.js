import { heightStyle } from '../../../helpers/height-style';
import { module, test } from 'qunit';

module('Unit | Helper | height style');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = heightStyle(42);
  assert.ok(result);
});
