import { widthStyle } from '../../../helpers/width-style';
import { module, test } from 'qunit';

module('Unit | Helper | width style');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = widthStyle(42);
  assert.ok(result);
});
